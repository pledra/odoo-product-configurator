<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- Product item used by /shop and /shop/cart -->
    <template id="products_item_product_price" name="Products item" inherit_id="website_sale.products_item">
        <xpath expr="//div[hasclass('product_price')]" position="after">
            <div itemprop="offers" itemscope="itemscope" itemtype="http://schema.org/Offer" class="product_price" t-if="not product.product_variant_ids and product.config_ok">
                <b>
                    <t t-if="product.website_price_difference and website.get_current_pricelist().discount_policy=='without_discount'">
                        <del class="text-danger mr8" style="white-space: nowrap;" t-esc="compute_currency(product.website_public_price)" t-options="{'widget': 'monetary', 'display_currency': website.get_current_pricelist().currency_id, 'from_currency': website.currency_id}" />
                    </t>

                    <span t-esc="product.website_price" t-options="{'widget': 'monetary', 'display_currency': website.currency_id}"/>
                    <span itemprop="price" style="display:none;" t-esc="product.website_price" />
                    <span itemprop="priceCurrency" style="display:none;" t-esc="website.currency_id.name" />
                </b>
            </div>
        </xpath>
    </template>

    <template id="error_page">
        <t t-call="website.layout">
            <div class="wrap">
                <div class="oe_structure">
                    <div class="container">
                        <div style="min-height: 200px; padding: 50px 50px;">
                            <div class="row">
                                <div t-if="error" class="col-lg-12 col-sm-12">
                                    <h1 class="mt32">ERROR!</h1>
                                    <h4><b><u>Something went wrong. Please try again later.</u></b></h4>
                                </div>
                                <div t-if="message" class="col-lg-12 col-sm-12">
                                    <h4><t t-esc="message"/></h4>
                                </div>
                            </div>
                            <div class="row">
                                <div class="col-lg-12 col-sm-12">
                                    <h4>
                                        <a t-attf-href="/shop">Go Home</a>
                                    </h4>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </t>
    </template>

</odoo>
